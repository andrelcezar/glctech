{
  "name": "glctech-site",
  "version": "1.0.0",
  "private": true,
  "description": "GLCTech Sec marketing website (static, multilingual) served by a Cloudflare Worker that also handles the contact and careers form endpoints.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "pretest": "node scripts/check-tests-exist.mjs",
    "test": "node --test \"tests/*.test.js\" \"tests/*.test.mjs\"",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "wrangler": "^3.90.0"
  }
}
